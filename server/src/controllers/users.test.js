const supertest = require("supertest");

describe("All CRUD capabilities are functioning", () => {
  test("POST to /users/signup is working as it should", () => {});
  test("POST to /users/userId is working as it should", () => {});
  test("DELETE to /users/login is working as it should", () => {});
});
