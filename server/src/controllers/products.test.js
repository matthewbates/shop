const supertest = require("supertest");

describe("Groups CRUD tests together", () => {
  test("GET /products is working as expected", () => {});
  test("GET /products/productId is working as expected", () => {});
  test("POST /products is working as expected", () => {});
  test("PATCH /products/productId is working as expected", () => {});
  test("DELETE /products/productId is working as expected", () => {});
});
