import styled from "styled-components";
import CONSTANTS from "../../utils/constants";

export const NavbarContainer = styled.div`
  width: 100%;
  height: 70px;
  background: ${CONSTANTS.colors.navy};
`;
